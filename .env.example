# Vercel → Project → Settings → Environment Variables
# Postmark: Servers → your server → API Tokens → "Server API token" (not Account token)
POSTMARK_API_TOKEN=your-server-api-token-here
# Must be a verified sender signature in Postmark (e.g. contact@aulmtrading.com)
FROM_EMAIL=contact@aulmtrading.com
